{
  "name": "server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development DISABLE_VITE_MIDDLEWARE=true PORT=5000 node --import tsx ../../server/index.ts",
    "build": "node --import tsx ../../script/build.ts",
    "check-types": "tsc --noEmit -p ../../tsconfig.json"
  }
}
